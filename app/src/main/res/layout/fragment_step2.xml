<?xml version="1.0" encoding="utf-8"?>

<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center_vertical"
        android:padding="24dp"
        android:background="@color/background">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Your Cycle 🌿"
            android:textSize="22sp"
            android:textStyle="bold"/>

        <EditText
            android:id="@+id/cycleLength"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Cycle Length (e.g. 28)"
            android:inputType="number"
            android:background="@drawable/bg_input"
            android:padding="14dp"
            android:layout_marginTop="20dp"/>

        <EditText
            android:id="@+id/periodLength"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Period Length (days)"
            android:inputType="number"
            android:background="@drawable/bg_input"
            android:padding="14dp"
            android:layout_marginTop="12dp"/>

        <EditText
            android:id="@+id/lastPeriodDate"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Last Period (YYYY-MM-DD)"
            android:background="@drawable/bg_input"
            android:padding="14dp"
            android:layout_marginTop="12dp"/>

        <Spinner
            android:id="@+id/regularity"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"/>

        <Button
            android:id="@+id/nextBtn"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Next"
            android:layout_marginTop="24dp"/>
    </LinearLayout>
</ScrollView>